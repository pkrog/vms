# vi: ft=ruby sw=0 ts=2 et fdm=marker

# Constants {{{1
################################################################

$os = 'freebsd'
$os_version = '12'
$box = "generic/#{$os}#{$os_version}"

# Main {{{1
################################################################

Vagrant.configure(2) do |config|
  
  config.vm.box = $box
end
